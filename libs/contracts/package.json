{
  "name": "@contracts/source",
  "version": "0.0.0",
  "private": true,
  "description": "Shared DTOs and SSE event types for opencode-chat. Consumed via the '@contracts/*' TypeScript path alias (tsconfig paths in apps/web, tsc-alias-rewritten in apps/api) — never imported through node_modules.",
  "main": "src/index.ts",
  "types": "src/index.ts"
}
