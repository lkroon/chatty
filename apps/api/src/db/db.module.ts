import { Module } from '@nestjs/common';

// Wave 1 workstream C owns this module: the drizzle-orm/pg connection
// (DATABASE_URL), schema, and migrations for `accounts` and `messages`.
// Empty stub for Wave 0 — do not add real logic here.
@Module({})
export class DbModule {}
