import { Component } from '@angular/core';

// Wave 1 workstream D owns this directory: the chat shell (conversation
// list, message thread, composer, SSE streaming via POST /api/chat).
// Minimal stub for Wave 0 — just enough for routing to resolve.
@Component({
  selector: 'app-chat-shell',
  imports: [],
  template: `<p>chat shell stub</p>`,
})
export class ChatShell {}
