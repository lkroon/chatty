import { Component } from '@angular/core';

// Wave 1 workstream E owns this directory: the login shell / app shell
// chrome (PWA manifest wiring, redirect-based Google OAuth entry point,
// top-level layout). Minimal stub for Wave 0 — just enough for routing
// to resolve.
@Component({
  selector: 'app-login-shell',
  imports: [],
  template: `<p>login shell stub</p>`,
})
export class LoginShell {}
